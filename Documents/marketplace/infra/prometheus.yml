# Prometheus config for marketplace (profile: observability)
# Add scrape_configs for Go services when they expose /metrics
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: prometheus
    static_configs:
      - targets: ["localhost:9090"]
